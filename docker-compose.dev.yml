# 2026-09-18, version 4.1.3-beta: ASGSR, Alexey Emanov
# Development overlay: enable Tornado debug/autoreload.
# Usage: docker compose -f docker-compose.yml -f docker-compose.dev.yml up

services:
    yasmine-backend:
        environment:
            YASMINE_DEBUG: "1"
        command: python yasmineapp.py syncdb upgrade heads ; python yasmineapp.py runserver --debug
